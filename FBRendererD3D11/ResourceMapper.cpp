#include "stdafx.h"
#include "ResourceMapper.h"
#include "RendererD3D11.h"

namespace fastbird{
	MapData ResourceMapper::Map(ID3D11Buffer* buffer, MAP_TYPE type, UINT subResource, MAP_FLAG flag){
		RendererD3D11::GetInstance().
	}
	void ResourceMapper::Unmap(ID3D11Buffer* buffer){

	}
}